import React, { FC } from "react";

type UserMediaCardProps = {
  userId: string;
};

const UserMediaCard: FC<UserMediaCardProps> = ({ userId }) => {
  return <div>UserMediaCard</div>;
};

export default UserMediaCard;
